import { FilterUniversityPipe } from './filter-university.pipe';

describe('FilterUniversityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUniversityPipe();
    expect(pipe).toBeTruthy();
  });
});
