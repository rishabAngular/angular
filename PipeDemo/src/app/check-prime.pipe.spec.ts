import { CheckPrimePipe } from './check-prime.pipe';

describe('CheckPrimePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckPrimePipe();
    expect(pipe).toBeTruthy();
  });
});
